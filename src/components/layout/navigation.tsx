"use client";
import Link from "next/link";
import {useEffect,useState} from "react";
import {Languages,Menu,X} from "lucide-react";
import {siteConfig} from "@/config/site";
import {Container} from "@/components/ui/container";
import {useI18n} from "@/i18n/provider";
export function Navigation(){const[open,setOpen]=useState(false);const{locale,setLocale,messages:m}=useI18n();useEffect(()=>{const close=(e:KeyboardEvent)=>e.key==="Escape"&&setOpen(false);window.addEventListener("keydown",close);return()=>window.removeEventListener("keydown",close)},[]);const links=[[m.common.products,"/products"],[m.common.how,"/#how"],[m.common.demo,"/#demo"],[m.common.pricing,"/#pricing"],[m.common.faq,"/#faq"]];const switcher=<div className="language-switch" aria-label="Language switcher"><Languages size={14}/><button className={locale==="en"?"active":""} onClick={()=>setLocale("en")} aria-pressed={locale==="en"}>EN</button><i/><button className={locale==="zh"?"active":""} onClick={()=>setLocale("zh")} aria-pressed={locale==="zh"}>中文</button></div>;return <header className="fixed inset-x-0 top-0 z-50 pt-3"><Container><nav aria-label="Main navigation" className="relative flex h-14 items-center justify-between rounded-2xl border border-white/80 bg-white/75 px-5 shadow-[0_8px_30px_rgba(20,30,60,.07)] backdrop-blur-2xl"><Link href="/" className="flex items-center gap-2 text-[13px] font-bold tracking-[.12em] text-ink"><span className="grid h-7 w-7 place-items-center rounded-lg bg-gradient-to-br from-[#1685f8] to-[#765bea] text-xs text-white">T</span> TOO YI HAO</Link><div className="hidden items-center gap-6 lg:flex">{links.map(x=><Link key={x[1]} href={x[1]} className="text-xs text-muted transition-colors hover:text-ink">{x[0]}</Link>)}{switcher}</div><a href={siteConfig.bookingHref} className="hidden rounded-full bg-ink px-5 py-2.5 text-xs font-medium text-white md:block">{m.common.book}</a><button className="grid h-10 w-10 place-items-center rounded-full border border-line lg:hidden" aria-label={open?"Close menu":"Open menu"} onClick={()=>setOpen(!open)}>{open?<X size={18}/>:<Menu size={18}/>}</button>{open&&<div className="absolute left-0 right-0 top-[72px] rounded-2xl border border-line bg-white p-4 shadow-soft lg:hidden">{links.map(x=><Link key={x[1]} href={x[1]} onClick={()=>setOpen(false)} className="block rounded-xl px-4 py-3">{x[0]}</Link>)}<div className="px-4 py-3">{switcher}</div><a href={siteConfig.bookingHref} className="mt-2 block rounded-xl bg-ink px-4 py-3 text-center text-white">{m.common.book}</a></div>}</nav></Container></header>}
