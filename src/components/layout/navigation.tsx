"use client";
import Link from "next/link";
import {useEffect,useState} from "react";
import {Languages,Menu,X} from "lucide-react";
import {Container} from "@/components/ui/container";
import {useI18n} from "@/i18n/provider";

export function Navigation(){const[open,setOpen]=useState(false);const{locale,setLocale}=useI18n();const z=locale==="zh";useEffect(()=>{const close=(e:KeyboardEvent)=>e.key==="Escape"&&setOpen(false);window.addEventListener("keydown",close);return()=>window.removeEventListener("keydown",close)},[]);const links=[[z?"AI Workers":"AI Workers","/#featured"],[z?"市集":"Marketplace","/#marketplace"],[z?"价格":"Pricing","/#pricing"],[z?"资源":"Resources","/#faq"]];return <header className="premium-header"><Container><nav aria-label="Main navigation" className="premium-nav"><Link href="/" className="wordmark">TOOYIHAO</Link><div className="desktop-links">{links.map(x=><Link key={x[1]} href={x[1]}>{x[0]}</Link>)}</div><div className="nav-actions"><div className="compact-language"><Languages/><button onClick={()=>setLocale(locale==="en"?"zh":"en")} aria-label="Switch language">{locale==="en"?"中文":"EN"}</button></div><Link className="signin" href="/#experience">{z?"登录":"Sign In"}</Link><Link className="nav-cta" href="/#experience">{z?"开始体验":"Start Experience"}</Link></div><button className="mobile-menu" aria-label={open?"Close menu":"Open menu"} aria-expanded={open} onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button>{open&&<div className="mobile-drawer">{links.map(x=><Link onClick={()=>setOpen(false)} key={x[1]} href={x[1]}>{x[0]}</Link>)}<button onClick={()=>setLocale(locale==="en"?"zh":"en")}>{locale==="en"?"切换至中文":"Switch to English"}</button><Link onClick={()=>setOpen(false)} className="nav-cta" href="/#experience">{z?"开始体验":"Start Experience"}</Link></div>}</nav></Container></header>}
